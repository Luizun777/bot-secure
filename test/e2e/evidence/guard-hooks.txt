# Evidencia real: test/e2e/guard-hooks.test.mjs
# Generado: 2026-09-04T04:24:24.417Z
# node v26.7.0 · darwin

### Preparación
workspace: <tmp>/guard-hooks-XXXX
backend: dev
frontend: ai-dev
artefactos: 21

### session-start con backend en dev
exit=0
additionalContext:
## bot-secure — workspace tienda (sensitive, modo clone)

Mapa de apps:
- backend → ./backend (backend, spring) rama: dev
- frontend → ./frontend (frontend, next) rama: ai-dev

BLOQUEOS ACTIVOS (todo prompt y herramienta se rechazan hasta resolverlos):
- La app backend está en la rama protegida "dev". Arreglo: git -C backend switch ai-dev

### statusline
exit=0
⛔ backend:dev | ✅ frontend:ai-dev* | sandbox | nivel 1 | clone

### prompt con bloqueo de rama
exit=2
bot-secure: sesión bloqueada. La app backend está en la rama protegida "dev". Arreglo: git -C backend switch ai-dev

### pre-tool `ls -la` con la sesión bloqueada
exit=0  deny: Sesión bloqueada por bot-secure: La app backend está en la rama protegida "dev". Arreglo: git -C backend switch ai-dev

### pre-tool `git -C backend switch ai-dev` (recuperación)
exit=0  permitido (sin decisión de deny)

### session-start con todo en ai-dev
exit=0
## bot-secure — workspace tienda (sensitive, modo clone)

Mapa de apps:
- backend → ./backend (backend, spring) rama: ai-dev
- frontend → ./frontend (frontend, next) rama: ai-dev

Sin bloqueos. Trabaja en ai-dev / ai/*. Nunca leas .env ni pegues secretos: usa .env.ai y __AI_PLACEHOLDER__X__.

### prompt con secreto falso
exit=2
bot-secure: el prompt contiene 1 posible(s) secreto(s)/PII (aws-access-key-id). No pegues valores reales: usa __AI_PLACEHOLDER__X__ o un valor falso.

### prompt normal
exit=0

### pre-tool Bash `cat backend/.env`
exit=0  deny: Lectura de recurso sensible denegada: backend/.env (env-file). Los secretos viven fuera del entorno de IA; usa .env.ai.

### pre-tool Bash con expansión de variable
exit=0  deny: Lectura de recurso sensible denegada: backend/.env (env-file). Los secretos viven fuera del entorno de IA; usa .env.ai.

### pre-tool Read fuera del workspace
exit=0  deny: Lectura fuera del workspace denegada: <repo>/package.json. Trabaja solo dentro de <ws>.

### pre-tool Read `~/.claude.json`
exit=0  deny: Lectura de recurso sensible denegada: ~/.claude.json (home-credentials). Los secretos viven fuera del entorno de IA; usa .env.ai.

### pre-tool Write con secreto
exit=0  deny: Contenido con posible secreto/PII (aws-access-key-id) denegado en backend/src/config.js. Usa __AI_PLACEHOLDER__X__ o deja el campo vacío; los valores de IA viven en .env.ai.

### pre-tool Write con `gitleaks:allow`
exit=0  deny: Escribir supresiones (gitleaks:allow, bot-secure:allow, allowlist secret) está denegado: usa bot-secure baseline add con revisión.

### pre-tool Write sobre .claude/settings.json
exit=0  deny: Escritura sobre las guardas denegada: .claude/settings.json. Cambia la política con bot-secure policy y pide revisión.

### pre-tool Bash redirigiendo a .claude/hooks/guard.mjs
exit=0  deny: Escritura sobre las guardas denegada: .claude/hooks/guard.mjs. Cambia la política con bot-secure policy y pide revisión.

### pre-tool `git -C backend switch dev`
exit=0  deny: Cambio a rama protegida denegado (dev). Trabaja en ai-dev o en ramas ai/*: git switch -c ai/mi-tarea

### pre-tool `git push origin ai/mi-tarea`
exit=0  permitido (sin decisión de deny)

### pre-tool `git push origin ai-dev` (solo CI)
exit=0  deny: Push denegado (origin ai-dev). Solo se permite git push origin ai/<rama>; ai-dev la actualiza CI.

### pre-tool `git commit --no-verify`
exit=0  deny: --no-verify denegado: los hooks git son parte de las guardas.

### pre-tool `curl api.anthropic.com.evil`
exit=0  deny: Egreso de red denegado (curl → api.anthropic.com.evil). Dominios permitidos: api.anthropic.com, repo.maven.apache.org, repo1.maven.org, plugins.gradle.org, services.gradle.org, registry.npmjs.org, localhost, 127.0.0.1, [::1]. Pide añadirlo en policy.network.allowedDomains.

### pre-tool `curl --resolve`
exit=0  deny: Egreso de red denegado: --resolve/--connect-to o IP literal no permitidos (curl).

### pre-tool `curl registry.npmjs.org` (registro permitido)
exit=0  permitido (sin decisión de deny)

### pre-tool `printenv`
exit=0  deny: Volcado de variables de entorno denegado (printenv). Usa env VAR=valor comando para variables concretas.

### pre-tool `node -e process.env`
exit=0  deny: Volcado de variables de entorno denegado (node). Usa env VAR=valor comando para variables concretas.

### pre-tool `env AI_ENV=1 npm test`
exit=0  permitido (sin decisión de deny)

### pre-tool desde subagente (agent_id=sub-1)
exit=0  deny: Lectura de recurso sensible denegada: backend/.env (env-file). Los secretos viven fuera del entorno de IA; usa .env.ai.

### pre-tool `mcp__zapier__send_email`
exit=0  deny: Servidor MCP no permitido: zapier. Permitidos: —. Añádelo en policy.mcp.allowed con revisión.

### config-change
exit=2
bot-secure: cambio de configuración de Claude Code bloqueado. Cambia la política con bot-secure policy compile.

### pre-tool tras alterar .claude/settings.json
exit=0  deny: INTEGRIDAD ROTA: .claude/settings.json no coinciden con lock.json. Arreglo: bot-secure doctor

### pre-tool tras recompilar (integridad restaurada)
exit=0  permitido (sin decisión de deny)

### lanzador con PATH vacío y BOT_SECURE_NODE inválido
exit=2
bot-secure: BOT_SECURE_NODE=<ws>/no-existe-node no es ejecutable; la guarda bloquea. Arreglo: bot-secure doctor

### lanzador con PATH vacío y sin guard.mjs
exit=2
bot-secure: falta <ws>/.claude/hooks/guard.mjs; la guarda bloquea. Arreglo: bot-secure init

### lanzador fuera de un workspace
exit=2
bot-secure: no encuentro el workspace (.bot-secure/policy.json) desde <repo>/test/e2e/tmp/sin-workspace-V0tDZZ. Arreglo: abre Claude Code en la raíz del workspace de IA

### session-end con sesión contaminada
exit=0
archivo: INCIDENT-2026-09-04T04-24-24-120Z.md
# Incidente bot-secure — 2026-09-04T04:24:24.120Z

- Sesión: s-test
- Workspace: tienda
- Transcript analizado: sí
- Intentos contra recursos sensibles: sí (integrity-drift)
- Hallazgos en el transcript: 0

## Qué hacer


### git pre-push hacia refs/heads/dev
exit=1
bot-secure[pre-push]: push a refs/heads/dev rechazado desde el entorno de IA. Abre un PR desde ai/<rama> hacia dev.

### git pre-commit con secreto staged
exit=1
bot-secure[pre-commit]: commit rechazado: 1 posible(s) secreto(s)/PII en archivos staged (config.js). Muévelos a .env (ignorado) o usa __AI_PLACEHOLDER__X__.

### git pre-commit con gitleaks:allow
exit=1
bot-secure[pre-commit]: commit rechazado: supresiones inline (gitleaks:allow / bot-secure:allow) en config.js. Usa bot-secure baseline add.

### stop con recordatorio de docs
1ª vez exit=2: bot-secure: tocaste código (1 archivo(s)) sin actualizar docs/. Actualiza docs/ o CLAUDE.md antes de terminar (solo se pide una vez).
2ª vez exit=0
