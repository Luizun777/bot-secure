# Historias de usuario — {{project}}

Lee esto para saber qué existe y qué está en curso. Crea una nueva con `/hu` (copia `_PLANTILLA-HU.md`, numera consecutivo y añádela aquí). Criterios en Gherkin `# language: es`. Nunca datos reales en ejemplos.

| ID | Título | Épica | Estado | Prioridad | Datos sensibles |
|---|---|---|---|---|---|
| HU-0001 | Ejemplo: alta de cliente con validación de RFC | ejemplo | borrador | media | RFC, correo, teléfono (sintéticos) |

Estados: `borrador` → `lista` → `en-curso` → `en-revision` → `hecha` (o `descartada`).
